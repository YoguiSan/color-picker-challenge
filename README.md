# Color picker challenge for TransPerfect
## Running the application

- On a terminal (in this folder), run ***npm install***
- After install is done, run ***nppm start***
- The application will be started on ***http://localhost:3000***